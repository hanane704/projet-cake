
{% extends 'base.html.twig' %}

{% block css %}<link rel="stylesheet" href="{{asset( "css/login.css")}}">{% endblock %}


{% block body %}
<div>
        <div class="m-5 text-center">
            <h1>Connexion</h1>
        </div>
        <div class="row">
            <div class="col m-2">
                <img src="image/kaak-warka-dentelle-495g.jpg" />
            </div>
            <div class="col m-2">
<form method="post">
    {% if error %}
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
            {{ error.messageKey|trans(error.messageData, 'security') }}
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    {% endif %}
 <div calss="containte">
    <h1 class="h3 mb-3 font-weight-normal">Identifiez-vous</h1>
     
                <form method="post">
                   <label><b>Email</b></label>
    <input type="email" value="{{ last_username }}" name="email" required >

    <label><b>Password</b></label>
    <input type="password" name="password"  required>
   
       

    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
  

    {#
        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
        See https://symfony.com/doc/current/security/remember_me.html

        <div class="checkbox mb-3">
            <label>
                <input type="checkbox" name="_remember_me"> Remember me
            </label>
        </div>
    #}

    <input class='lf--submit' type='submit' value='LOGIN'>


    <p>Pas de compte ? <a class=class='lf--forgot' href="{{ path('app_register') }}">créer un compte</a></p>
</form>
</div>
{% endblock %}